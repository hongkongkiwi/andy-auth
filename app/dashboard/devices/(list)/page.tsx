import DeviceListingPage from './_components/DeviceListingPage';

export const metadata = {
  title: 'Dashboard : Devices'
};

const Page = () => {
  return <DeviceListingPage />;
};

export default Page;
