'use client';

import type { ClientTableData } from './types';

interface CellActionProps {
  data: ClientTableData;
}

export const CellAction = ({ data }: CellActionProps) => {
  // Your cell action implementation
  return <div>{/* Action buttons */}</div>;
};
