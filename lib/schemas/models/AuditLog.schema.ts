/******************************************************************************
 * This file was generated by ZenStack CLI.
 ******************************************************************************/

/* eslint-disable */
// @ts-nocheck

import { z } from 'zod';
import { AuditSeverityLevel } from '@zenstackhq/runtime/models';
import { AuditLogEventTypeSchema } from '../enums/AuditLogEventType.schema';
import { AuditLogResourceTypeSchema } from '../enums/AuditLogResourceType.schema';
import { AuditSeverityLevelSchema } from '../enums/AuditSeverityLevel.schema';
const baseSchema = z
  .object({
    createdAt: z.coerce.date().default(() => new Date()),
    updatedAt: z.coerce.date(),
    createdBy: z.string().nullish(),
    updatedBy: z.string().nullish(),
    id: z.string(),
    eventType: AuditLogEventTypeSchema,
    resourceType: AuditLogResourceTypeSchema,
    resourceId: z.string(),
    userId: z.string(),
    severity: AuditSeverityLevelSchema,
    description: z.string(),
    metadata: z.any().nullish(),
    success: z.boolean().default(true)
  })
  .strict();

/**
 * `AuditLog` schema excluding foreign keys and relations.
 */
export const AuditLogScalarSchema = baseSchema;

/**
 * `AuditLog` schema including all fields (scalar, foreign key, and relations) and validations.
 */
export const AuditLogSchema = AuditLogScalarSchema;

/**
 * Schema used for validating Prisma create input. For internal use only.
 * @private
 */
export const AuditLogPrismaCreateSchema = baseSchema.partial().passthrough();

/**
 * Schema used for validating Prisma update input. For internal use only.
 * @private
 */
export const AuditLogPrismaUpdateSchema = z
  .object({
    createdAt: z.coerce.date().default(() => new Date()),
    updatedAt: z.coerce.date(),
    createdBy: z.string().nullish(),
    updatedBy: z.string().nullish(),
    id: z.string(),
    eventType: AuditLogEventTypeSchema,
    resourceType: AuditLogResourceTypeSchema,
    resourceId: z.string(),
    userId: z.string(),
    severity: AuditSeverityLevelSchema,
    description: z.string(),
    metadata: z.any().nullish(),
    success: z.boolean().default(true)
  })
  .partial()
  .passthrough();

/**
 * `AuditLog` schema for create operations excluding foreign keys and relations.
 */
export const AuditLogCreateScalarSchema = baseSchema.partial({
  createdAt: true,
  updatedAt: true,
  id: true,
  severity: true,
  success: true
});

/**
 * `AuditLog` schema for create operations including scalar fields, foreign key fields, and validations.
 */
export const AuditLogCreateSchema = baseSchema.partial({
  createdAt: true,
  updatedAt: true,
  id: true,
  severity: true,
  success: true
});

/**
 * `AuditLog` schema for update operations excluding foreign keys and relations.
 */
export const AuditLogUpdateScalarSchema = baseSchema.partial();

/**
 * `AuditLog` schema for update operations including scalar fields, foreign key fields, and validations.
 */
export const AuditLogUpdateSchema = AuditLogUpdateScalarSchema;
