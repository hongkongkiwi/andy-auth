/******************************************************************************
 * This file was generated by ZenStack CLI.
 ******************************************************************************/

/* eslint-disable */
// @ts-nocheck

import { z } from 'zod';
import { EntityStatus, Role } from '@zenstackhq/runtime/models';
import { RoleSchema } from '../enums/Role.schema';
import { PermissionSchema } from '../enums/Permission.schema';
const baseSchema = z
  .object({
    createdAt: z.coerce.date().default(() => new Date()),
    updatedAt: z.coerce.date(),
    createdBy: z.string().nullish(),
    updatedBy: z.string().nullish(),
    id: z.string(),
    role: RoleSchema,
    permissions: z.array(PermissionSchema)
  })
  .strict();
const relationSchema = z.object({
  user: z.record(z.unknown()),
  workspace: z.record(z.unknown()).optional(),
  client: z.record(z.unknown()).optional(),
  patrol: z.record(z.unknown()).optional()
});
const fkSchema = z.object({
  userId: z.string(),
  workspaceId: z.string().nullish(),
  clientId: z.string().nullish(),
  patrolId: z.string().nullish()
});

/**
 * `ResourceMember` schema excluding foreign keys and relations.
 */
export const ResourceMemberScalarSchema = baseSchema;

/**
 * `ResourceMember` schema including all fields (scalar, foreign key, and relations) and validations.
 */
export const ResourceMemberSchema = ResourceMemberScalarSchema.merge(
  fkSchema
).merge(relationSchema.partial());

/**
 * Schema used for validating Prisma create input. For internal use only.
 * @private
 */
export const ResourceMemberPrismaCreateSchema = baseSchema
  .partial()
  .passthrough();

/**
 * Schema used for validating Prisma update input. For internal use only.
 * @private
 */
export const ResourceMemberPrismaUpdateSchema = z
  .object({
    createdAt: z.coerce.date().default(() => new Date()),
    updatedAt: z.coerce.date(),
    createdBy: z.string().nullish(),
    updatedBy: z.string().nullish(),
    id: z.string(),
    role: RoleSchema,
    permissions: z.array(PermissionSchema)
  })
  .partial()
  .passthrough();

/**
 * `ResourceMember` schema for create operations excluding foreign keys and relations.
 */
export const ResourceMemberCreateScalarSchema = baseSchema.partial({
  createdAt: true,
  updatedAt: true,
  id: true,
  permissions: true
});

/**
 * `ResourceMember` schema for create operations including scalar fields, foreign key fields, and validations.
 */
export const ResourceMemberCreateSchema =
  ResourceMemberCreateScalarSchema.merge(fkSchema);

/**
 * `ResourceMember` schema for update operations excluding foreign keys and relations.
 */
export const ResourceMemberUpdateScalarSchema = baseSchema.partial();

/**
 * `ResourceMember` schema for update operations including scalar fields, foreign key fields, and validations.
 */
export const ResourceMemberUpdateSchema =
  ResourceMemberUpdateScalarSchema.merge(fkSchema.partial());
