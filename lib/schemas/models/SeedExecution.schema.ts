/******************************************************************************
 * This file was generated by ZenStack CLI.
 ******************************************************************************/

/* eslint-disable */
// @ts-nocheck

import { z } from 'zod';
import { SeedExecutionStatus } from '@zenstackhq/runtime/models';
import { SeedExecutionStatusSchema } from '../enums/SeedExecutionStatus.schema';
const baseSchema = z
  .object({
    createdAt: z.coerce.date().default(() => new Date()),
    updatedAt: z.coerce.date(),
    createdBy: z.string().nullish(),
    updatedBy: z.string().nullish(),
    id: z.string(),
    seedName: z.string().min(1).max(100),
    executedAt: z.coerce.date().default(() => new Date()),
    completedAt: z.coerce.date().nullish(),
    status: SeedExecutionStatusSchema,
    error: z.string().min(1).max(1000).nullish(),
    metadata: z.any().nullish()
  })
  .strict();

/**
 * `SeedExecution` schema excluding foreign keys and relations.
 */
export const SeedExecutionScalarSchema = baseSchema;

/**
 * `SeedExecution` schema including all fields (scalar, foreign key, and relations) and validations.
 */
export const SeedExecutionSchema = SeedExecutionScalarSchema;

/**
 * Schema used for validating Prisma create input. For internal use only.
 * @private
 */
export const SeedExecutionPrismaCreateSchema = baseSchema
  .partial()
  .passthrough();

/**
 * Schema used for validating Prisma update input. For internal use only.
 * @private
 */
export const SeedExecutionPrismaUpdateSchema = z
  .object({
    createdAt: z.coerce.date().default(() => new Date()),
    updatedAt: z.coerce.date(),
    createdBy: z.string().nullish(),
    updatedBy: z.string().nullish(),
    id: z.string(),
    seedName: z.string().min(1).max(100),
    executedAt: z.coerce.date().default(() => new Date()),
    completedAt: z.coerce.date().nullish(),
    status: SeedExecutionStatusSchema,
    error: z.string().min(1).max(1000).nullish(),
    metadata: z.any().nullish()
  })
  .partial()
  .passthrough();

/**
 * `SeedExecution` schema for create operations excluding foreign keys and relations.
 */
export const SeedExecutionCreateScalarSchema = baseSchema.partial({
  createdAt: true,
  updatedAt: true,
  id: true,
  executedAt: true,
  status: true
});

/**
 * `SeedExecution` schema for create operations including scalar fields, foreign key fields, and validations.
 */
export const SeedExecutionCreateSchema = baseSchema.partial({
  createdAt: true,
  updatedAt: true,
  id: true,
  executedAt: true,
  status: true
});

/**
 * `SeedExecution` schema for update operations excluding foreign keys and relations.
 */
export const SeedExecutionUpdateScalarSchema = baseSchema.partial();

/**
 * `SeedExecution` schema for update operations including scalar fields, foreign key fields, and validations.
 */
export const SeedExecutionUpdateSchema = SeedExecutionUpdateScalarSchema;
