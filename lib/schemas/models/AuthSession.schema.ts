/******************************************************************************
 * This file was generated by ZenStack CLI.
 ******************************************************************************/

/* eslint-disable */
// @ts-nocheck

import { z } from 'zod';
import { UserStatus } from '@zenstackhq/runtime/models';
const baseSchema = z
  .object({
    createdAt: z.coerce.date().default(() => new Date()),
    updatedAt: z.coerce.date(),
    createdBy: z.string().nullish(),
    updatedBy: z.string().nullish(),
    id: z.string(),
    token: z.string(),
    expiresAt: z.coerce.date(),
    ipAddress: z.string().nullish(),
    userAgent: z.string().nullish(),
    lastActiveAt: z.coerce.date().nullish(),
    isRevoked: z.boolean().default(false),
    deviceId: z.string().nullish(),
    deviceName: z.string().nullish(),
    sessionType: z.string().default('web'),
    geoPatrol: z.any().nullish(),
    mfaVerified: z.boolean().default(false),
    failedAttempts: z.number().default(0),
    refreshToken: z.string().nullish()
  })
  .strict();
const relationSchema = z.object({
  user: z.record(z.unknown())
});
const fkSchema = z.object({
  userId: z.string()
});

/**
 * `AuthSession` schema excluding foreign keys and relations.
 */
export const AuthSessionScalarSchema = baseSchema;

/**
 * `AuthSession` schema including all fields (scalar, foreign key, and relations) and validations.
 */
export const AuthSessionSchema = AuthSessionScalarSchema.merge(fkSchema).merge(
  relationSchema.partial()
);

/**
 * Schema used for validating Prisma create input. For internal use only.
 * @private
 */
export const AuthSessionPrismaCreateSchema = baseSchema.partial().passthrough();

/**
 * Schema used for validating Prisma update input. For internal use only.
 * @private
 */
export const AuthSessionPrismaUpdateSchema = z
  .object({
    createdAt: z.coerce.date().default(() => new Date()),
    updatedAt: z.coerce.date(),
    createdBy: z.string().nullish(),
    updatedBy: z.string().nullish(),
    id: z.string(),
    token: z.string(),
    expiresAt: z.coerce.date(),
    ipAddress: z.string().nullish(),
    userAgent: z.string().nullish(),
    lastActiveAt: z.coerce.date().nullish(),
    isRevoked: z.boolean().default(false),
    deviceId: z.string().nullish(),
    deviceName: z.string().nullish(),
    sessionType: z.string().default('web'),
    geoPatrol: z.any().nullish(),
    mfaVerified: z.boolean().default(false),
    failedAttempts: z.union([z.number().default(0), z.record(z.unknown())]),
    refreshToken: z.string().nullish()
  })
  .partial()
  .passthrough();

/**
 * `AuthSession` schema for create operations excluding foreign keys and relations.
 */
export const AuthSessionCreateScalarSchema = baseSchema.partial({
  createdAt: true,
  updatedAt: true,
  id: true,
  isRevoked: true,
  sessionType: true,
  mfaVerified: true,
  failedAttempts: true
});

/**
 * `AuthSession` schema for create operations including scalar fields, foreign key fields, and validations.
 */
export const AuthSessionCreateSchema =
  AuthSessionCreateScalarSchema.merge(fkSchema);

/**
 * `AuthSession` schema for update operations excluding foreign keys and relations.
 */
export const AuthSessionUpdateScalarSchema = baseSchema.partial();

/**
 * `AuthSession` schema for update operations including scalar fields, foreign key fields, and validations.
 */
export const AuthSessionUpdateSchema = AuthSessionUpdateScalarSchema.merge(
  fkSchema.partial()
);
