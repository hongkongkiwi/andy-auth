/******************************************************************************
 * This file was generated by ZenStack CLI.
 ******************************************************************************/

/* eslint-disable */
// @ts-nocheck

import { z } from 'zod';
import { UserStatus } from '@zenstackhq/runtime/models';
const baseSchema = z
  .object({
    createdAt: z.coerce.date().default(() => new Date()),
    updatedAt: z.coerce.date(),
    createdBy: z.string().nullish(),
    updatedBy: z.string().nullish(),
    id: z.string(),
    provider: z.string(),
    providerAccountId: z.string(),
    accessToken: z.string().nullish(),
    refreshToken: z.string().nullish(),
    expiresAt: z.coerce.date().nullish(),
    scope: z.string().nullish(),
    idToken: z.string().nullish(),
    tokenType: z.string().nullish(),
    sessionState: z.string().nullish()
  })
  .strict();
const relationSchema = z.object({
  user: z.record(z.unknown())
});
const fkSchema = z.object({
  userId: z.string()
});

/**
 * `OAuthAccount` schema excluding foreign keys and relations.
 */
export const OAuthAccountScalarSchema = baseSchema;

/**
 * `OAuthAccount` schema including all fields (scalar, foreign key, and relations) and validations.
 */
export const OAuthAccountSchema = OAuthAccountScalarSchema.merge(
  fkSchema
).merge(relationSchema.partial());

/**
 * Schema used for validating Prisma create input. For internal use only.
 * @private
 */
export const OAuthAccountPrismaCreateSchema = baseSchema
  .partial()
  .passthrough();

/**
 * Schema used for validating Prisma update input. For internal use only.
 * @private
 */
export const OAuthAccountPrismaUpdateSchema = z
  .object({
    createdAt: z.coerce.date().default(() => new Date()),
    updatedAt: z.coerce.date(),
    createdBy: z.string().nullish(),
    updatedBy: z.string().nullish(),
    id: z.string(),
    provider: z.string(),
    providerAccountId: z.string(),
    accessToken: z.string().nullish(),
    refreshToken: z.string().nullish(),
    expiresAt: z.coerce.date().nullish(),
    scope: z.string().nullish(),
    idToken: z.string().nullish(),
    tokenType: z.string().nullish(),
    sessionState: z.string().nullish()
  })
  .partial()
  .passthrough();

/**
 * `OAuthAccount` schema for create operations excluding foreign keys and relations.
 */
export const OAuthAccountCreateScalarSchema = baseSchema.partial({
  createdAt: true,
  updatedAt: true,
  id: true
});

/**
 * `OAuthAccount` schema for create operations including scalar fields, foreign key fields, and validations.
 */
export const OAuthAccountCreateSchema =
  OAuthAccountCreateScalarSchema.merge(fkSchema);

/**
 * `OAuthAccount` schema for update operations excluding foreign keys and relations.
 */
export const OAuthAccountUpdateScalarSchema = baseSchema.partial();

/**
 * `OAuthAccount` schema for update operations including scalar fields, foreign key fields, and validations.
 */
export const OAuthAccountUpdateSchema = OAuthAccountUpdateScalarSchema.merge(
  fkSchema.partial()
);
